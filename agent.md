# Agent Compatibility Note

Canonical instructions are defined in `/Users/alex/Documents/tts-trying/AGENTS.md`.

If a tool expects `agent.md`, apply the same rules:
- commands-first workflow,
- strict boundaries,
- test-before-commit,
- structured commits and frequent pushes,
- no secret leakage.
